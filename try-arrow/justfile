# -*-Makefile-*-

run: install
    build/try-arrow

example: install
    build/arrow-example

install:
    meson setup build src
    meson compile -C build
    meson install -C build

clean:
    rm build ../install/try-arrow -rf
